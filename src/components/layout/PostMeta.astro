---
import { Icon } from "astro-icon/components";
import { analyticsConfig, commentConfig } from "@/config";
import I18nKey from "@/i18n/i18nKey";
import { i18n } from "@/i18n/translation";
import { formatDateToYYYYMMDD } from "@/utils/date-utils";
import { getCategoryUrl, getTagUrl } from "@/utils/url-utils";

export interface Props {
	published: Date;
	updated?: Date;
	category?: string;
	tags?: string[];
	hideUpdateDate?: boolean;
	hideTagsForMobile?: boolean;
	isHome?: boolean;
	className?: string;
	id?: string;
	showPublished?: boolean;
	showCategory?: boolean;
	showTags?: boolean;
	maxTags?: number;
	showNoTags?: boolean;
	customPath?: string;
	pinned?: boolean;
	password?: boolean;
}

const {
	published,
	updated,
	category,
	tags,
	hideUpdateDate,
	hideTagsForMobile,
	isHome,
	className = "",
	id,
	showPublished = true,
	showCategory = true,
	showTags = true,
	maxTags,
	showNoTags = true,
	customPath,
	pinned,
} = Astro.props;

const path = customPath || (id ? `/posts/${id}` : "");
const visibleTags =
	typeof maxTags === "number" ? tags?.slice(0, maxTags) : tags;
const umamiAnalytics = analyticsConfig.umamiAnalytics;
const useUmamiPageViews = Boolean(
	!isHome &&
	id &&
	umamiAnalytics?.websiteId &&
	umamiAnalytics.shareId &&
	umamiAnalytics.shareApiBase &&
	umamiAnalytics.showPageViews !== false,
);
---

<div
  class:list={[
    "post-meta-root flex min-w-0 max-w-full flex-wrap text-neutral-500 dark:text-neutral-400 items-center gap-4 gap-x-4 gap-y-2",
    className,
  ]}
>
  {
    pinned && (
      <div class="pinned-btn flex items-center gap-1 bg-(--btn-regular-bg) text-(--btn-content) rounded-md px-2 py-1.5 font-bold">
        <Icon is:inline name="material-symbols:pinboard" class="text-xl" />
        <span class="text-sm">{i18n(I18nKey.pinned)}</span>
      </div>
    )
  }

  {
    showPublished && (
      <div class="flex items-center">
        <div class="meta-icon">
          <Icon
            is:inline
            name="material-symbols:calendar-today-outline-rounded"
            class="text-xl"
          />
        </div>
        <span class="text-50 text-sm font-medium">
          {formatDateToYYYYMMDD(published)}
        </span>
      </div>
    )
  }

  {
    showPublished &&
      !hideUpdateDate &&
      updated &&
      updated.getTime() !== published.getTime() && (
        <div class="flex items-center">
          <div class="meta-icon">
            <Icon
              is:inline
              name="material-symbols:edit-calendar-outline-rounded"
              class="text-xl"
            />
          </div>
          <span class="text-50 text-sm font-medium">
            {formatDateToYYYYMMDD(updated)}
          </span>
        </div>
      )
  }

  {
    showCategory && (
      <div class="flex items-center">
        <div class="meta-icon">
          <Icon is:inline name="material-symbols:book-2-outline-rounded" class="text-xl" />
        </div>
        <div class="flex flex-row flex-nowrap items-center">
          <a
            href={getCategoryUrl(category || "")}
            aria-label={`View all posts in the ${category} category`}
            class="link-lg transition text-50 text-sm font-medium
              hover:text-(--primary) dark:hover:text-(--primary) whitespace-nowrap"
          >
            {category || i18n(I18nKey.uncategorized)}
          </a>
        </div>
      </div>
    )
  }

  {
    showTags && (
      <div
        class:list={[
          "post-meta-tags min-w-0 max-w-full items-center",
          { flex: !hideTagsForMobile, "hidden md:flex": hideTagsForMobile },
        ]}
      >
        <div class="meta-icon">
          <Icon is:inline name="material-symbols:tag-rounded" class="text-xl" />
        </div>
        <div class="post-meta-tags-list flex min-w-0 max-w-full flex-row flex-wrap items-center gap-y-1 md:flex-nowrap">
          {visibleTags &&
            visibleTags.length > 0 &&
            visibleTags.map((tag, i) => (
              <>
                <div
                  class:list={[
                    { hidden: i === 0 },
                    "mx-1.5 text-(--meta-divider) text-sm",
                  ]}
                >
                  /
                </div>
                <a
                  href={getTagUrl(tag)}
                  aria-label={`View all posts with the ${tag.trim()} tag`}
                  class="link-lg transition text-50 text-sm font-medium
                    hover:text-(--primary) dark:hover:text-(--primary) break-words md:whitespace-nowrap"
                >
                  {tag.trim()}
                </a>
              </>
            ))}
          {showNoTags && !(visibleTags && visibleTags.length > 0) && (
            <div class="transition text-50 text-sm font-medium">
              {i18n(I18nKey.noTags)}
            </div>
          )}
        </div>
      </div>
    )
  }

  {
    useUmamiPageViews && (
      <div class="flex items-center">
        <div class="meta-icon">
          <Icon
            is:inline
            name="material-symbols:visibility-outline-rounded"
            class="text-xl"
          />
        </div>
        <span class="text-50 text-sm font-medium mr-1">
          {i18n(I18nKey.pageViews)}
        </span>
        <span
          class="text-50 text-sm font-medium"
          data-umami-pageviews={path}
          aria-live="polite"
        >
          …
        </span>
      </div>
    )
  }

  {
    !isHome &&
      commentConfig.type === "twikoo" &&
      commentConfig.twikoo?.visitorCount &&
      !useUmamiPageViews &&
      id && (
        <div class="flex items-center">
          <div class="meta-icon">
            <Icon
              is:inline
              name="material-symbols:visibility-outline-rounded"
              class="text-xl"
            />
          </div>
          <span class="text-50 text-sm font-medium mr-1">
            {i18n(I18nKey.pageViews)}
          </span>
          <span class="text-50 text-sm font-medium" id="twikoo_visitors">
            {i18n(I18nKey.pageViewsLoading)}
          </span>
        </div>
      )
  }

  {
    !isHome &&
      commentConfig.type === "waline" &&
      commentConfig.waline?.visitorCount &&
      !useUmamiPageViews &&
      id && (
        <div class="flex items-center">
          <div class="meta-icon">
            <Icon
              is:inline
              name="material-symbols:visibility-outline-rounded"
              class="text-xl"
            />
          </div>
          <span class="text-50 text-sm font-medium mr-1">
            {i18n(I18nKey.pageViews)}
          </span>
          <span
            class="text-50 text-sm font-medium waline-pageview-count"
            data-path={path}
          >
            {i18n(I18nKey.pageViewsLoading)}
          </span>
        </div>
      )
  }

  {
    !isHome &&
      commentConfig.type === "artalk" &&
      commentConfig.artalk?.visitorCount &&
      !useUmamiPageViews &&
      id && (
        <div class="flex items-center">
          <div class="meta-icon">
            <Icon
              is:inline
              name="material-symbols:visibility-outline-rounded"
              class="text-xl"
            />
          </div>
          <span class="text-50 text-sm font-medium mr-1">
            {i18n(I18nKey.pageViews)}
          </span>
          <span
            class="text-50 text-sm font-medium artalk-pv-count"
            data-path={path}
          >
            {i18n(I18nKey.pageViewsLoading)}
          </span>
        </div>
      )
  }
</div>
