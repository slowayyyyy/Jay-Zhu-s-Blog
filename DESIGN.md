# 苍蓝回廊设计规范

## 1. 设计边界

- 工程、路由、内容集合、文章组件、搜索、归档、图库、友链、Bilibili、Markdown 和 Swup 以 Firefly `6.16.6` 为准。
- Aemeath 只提供功能与构图参考，不复制其作者素材、文案、壁纸、社交数据或整套页面结构。
- 自定义集中在品牌主题、开场、首页信息层级、关于页和完整音乐页；不要把每个 Firefly 页面重新实现一遍。
- 洛琪希图像是主题主视觉，不用于角色扮演式文案。导航和管理字段始终使用直白中文。

## 2. 视觉语言

核心意象是水、月光、薄雾、书页和低亮度魔法回路。页面应有梦幻感，但正文、表单和状态必须清楚。

### 色彩

| Token | 亮色 | 用途 |
| --- | --- | --- |
| `--azure-moon` | `#edf5f6` | 页面底色 |
| `--azure-frost` | `#f8fbfb` | 卡片与阅读面 |
| `--azure-ink` | `#152c3a` | 主文字 |
| `--azure-ink-soft` | `#526b78` | 次文字 |
| `--azure-water` | `#256f82` | 主交互色 |
| `--azure-glacial` | `#87c8d5` | 水纹与高光 |
| `--azure-deep` | `#071925` | 深色主视觉 |
| `--azure-focus` | `#e9b96b` | 少量暖色焦点 |

深色页面以 `#041019` 为底、`#e4f1f3` 为主文字、`#75d3e2` 为交互色。暖色只能作为微量焦点，不能变成第二套主色。

### 字体

- 展示标题：`Noto Serif SC Variable`，用于首页、关于、音乐页和大标题。
- 正文与界面：`Noto Sans SC Variable`。
- 代码：沿用 Firefly 的等宽字体配置。
- 中文正文保持正常字重和舒适行高，不用过细银灰字。

### 形状与材质

- 内容页主要圆角为 `1rem`，视觉主舞台可使用 `1.75rem` 或不对称圆角。
- 普通文章列表不堆叠玻璃卡片；首页使用开放时间线。
- 玻璃效果只用于导航、开场按钮和少数浮层，必须有清晰边框与文字对比。
- 图像、表格和公式相对正文中心对称扩展，不得单侧溢出。

## 3. 动效语法

- 水纹：点击与进入动作。
- 折射或圆形收束：开场进入与页面层级变化。
- 缓慢漂移：壁纸和主视觉，周期不少于 9 秒。
- 消散：歌词、短句和记忆类内容的更替。
- 文章悬停只做轻微横移与节点扩散，不放大整张卡片造成布局抖动。
- 所有核心动效遵守 `prefers-reduced-motion`；关闭动效后内容和操作仍完整。

## 4. 页面规则

### 开场

- 每个浏览会话首次进入主页显示一次。
- 使用 `opening-roxy-720p.mp4`，静音循环，R2 托管；失败时显示水系静态封面。
- 只保留一个主操作“步入回廊”，点击后收束退场并尝试启动音乐。

### 首页

- 首屏采用 Firefly 壁纸组件、三套统一洛琪希场景和原生轮播。
- 最近文章为开放时间线，按内容自然增长。
- “回廊时刻”只展示本地时间、每日短句和用户主动授权的天气，不读取 IP 位置。
- 乐境、画廊、说说、追番和关于使用不同构图，不能回退为相同等宽卡片。

### 文章与归档

- 文章阅读优先于主题装饰。
- 保留 Firefly 原生目录、代码、KaTeX、Mermaid、图片灯箱、搜索和 RSS。
- 归档沿用 Firefly 筛选逻辑；搜索只显示真实匹配。

### 乐境

- 独立页与迷你播放器共享 `window.__fireflyMusic`，切页不得创建第二个音频实例。
- 唱片、歌词、歌单、进度、音量和播放模式均由同一状态驱动。

### 关于

- 是全站视觉表现最强的内页。
- 洛琪希素材负责世界氛围，原头像负责真实作者身份，两者不能互相替代。
- 自我介绍、研究兴趣、爱好、当前事项和联系入口由后台维护。

### 图库、友链与追番

- 图库和友链使用 Firefly 原生页面，数据改为后台可编辑 JSON。
- 追番使用 Firefly 原生 Bilibili 页面；不接入 TMDB，不新增本地番剧页。

## 5. 后台与内容

- Decap CMS 是管理界面底座，配置和增强脚本保存在本仓库。
- 所有公开内容必须由前后台共同读取同一份文件，禁止另建只在浏览器本地生效的数据源。
- 文章正文继续使用 Markdown；图片粘贴、图注、LaTeX、Mermaid、上标和下标由 CMS 增强脚本处理。
- 音乐上传到 R2，文章图片和头像默认进入 `public/uploads`。
- 修改数据结构时必须同步更新：类型、前台读取、CMS 字段、构建测试和迁移文档。

## 6. 验收基线

1. `pnpm astro check` 为 0 errors、0 warnings、0 hints。
2. `pnpm build` 成功生成首页、10 篇文章、搜索和 RSS。
3. 桌面与 390px 移动端检查首页、文章、乐境、关于和后台。
4. 深浅色与站内导航后状态保持，音乐切页不断流。
5. 开场视频失败、Bilibili UID 为空、Umami ID 为空、图库或友链无数据时都有可理解的降级状态。
