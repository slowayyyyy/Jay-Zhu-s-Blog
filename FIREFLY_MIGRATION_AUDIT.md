# Firefly / Aemeath 功能与迁移审计

> 状态：等待功能取舍确认，尚未开始替换现有博客代码。
>
> 审计日期：2026-09-02

## 1. 审计基线

| 项目 | 锁定版本 | 结论 |
| --- | --- | --- |
| 当前博客 | `7ed7a1bc5651883f1570f4c46fcb96c16ab4bcf6` | “灯火版”保留为可恢复版本 |
| Firefly | `f7c93c932c3241239f6f0d330e0b5c1bc701ce45` / `6.16.6` | 新博客的工程底座 |
| Aemeath | `47c9fdcccb5d684a7e81689fc08d2586540440f4` / 基于 `6.13.5` | 只移植选中的增量设计，不整体覆盖 |

Aemeath 落后当前 Firefly 三个小版本，并替换或删去了部分新版依赖。直接把 Aemeath 作为底座会丢失新版 Firefly 已有的 Markdown、图片、动态、配置和组件能力。因此采用以下原则：

1. 最新 Firefly 负责路由、内容集合、响应式布局、Markdown 和基础交互。
2. Aemeath 只作为功能与动效参考，选中的功能在最新版 Firefly 上重新实现。
3. 不复制 Aemeath 作者的文章、角色图、壁纸、音乐、友链截图和个人文案。
4. “灯火版”保持可恢复；“苍蓝回廊版”在独立开发分支实施。

## 2. Aemeath 增量功能取舍矩阵

决策值建议使用：`保留`、`替换`、`暂缓`、`删除`。

| 编号 | Aemeath 增量或深度改造 | 实际行为与风险 | 建议 | 待确认 |
| --- | --- | --- | --- | --- |
| A01 | 首页作品集式开场动画 | 角色立绘、上下场景横幅、分镜退场；支持会话内只播放一次、资源解码等待、移动端素材和动效降级 | **保留并重做**。使用洛琪希视频/立绘，改造成“苍蓝回廊”入场；保留可靠的生命周期逻辑，不照搬视觉 | `保留` |
| A02 | 独立番剧页 `/anime` | 聚合 Bilibili 追番和 TMDB 列表，带搜索、筛选、排序、分页、详情弹窗；构建时依赖外部接口 | **替换数据层**。保留卡片、筛选和详情体验，改为后台可维护的本地番剧集合，避免接口失效 | `替换` |
| A03 | “朋友圈” `/moments` | 不是个人说说，而是抓取友链 RSS/Atom 的文章聚合；带重试、快照和随机文章 | **不作为说说使用**。个人说说直接使用新版 Firefly `/dynamic`；朋友圈以后有需要再加 | `删除/暂缓` |
| A04 | 工具页 `/tools` | 工具数据全部硬编码，内容是 Aemeath 作者自己的项目和第三方工具 | **暂缓**。未来若需要“项目/工具”页，重建为后台内容集合 | `暂缓` |
| A05 | 更新日志 `/changelog` | 版本列表和大段 HTML 通知硬编码，并由脚本切换 | **删除复杂实现**。需要时改为简单的后台“站点日志”集合 | `删除` |
| A06 | 服务器状态 API | 读取 Node 主机 CPU、内存、磁盘和系统信息 | **删除**。Cloudflare Pages 没有可供展示的传统服务器主机，该实现也不适合边缘运行环境 | `删除` |
| A07 | 访客欢迎 Toast | 按时间问候，并调用第三方 IP 接口推断访客地区 | **删除地区查询**。开场已承担欢迎作用，避免隐私和第三方依赖；可只保留本地时间问候 | `替换/删除` |
| A08 | 实时时钟与问候侧栏 | 显示时间、时段问候并随机切换壁纸 | **替换**为低干扰的“回廊时刻”组件，最多展示时间、天气或一句诗，不做随机壁纸堆叠 | `替换` |
| A09 | 每日一言侧栏 | 10 条硬编码句子，按日期轮换 | **替换**为后台可编辑的短句池；首页只保留一个安静入口 | `替换` |
| A10 | 年/月/周进度与节日倒计时 | 仪表盘式进度和节日计算 | **删除**。与“苍蓝回廊”的阅读氛围冲突，也会重新引入打卡压力 | `删除` |
| A11 | 壁纸切换 | 多套桌面/手机壁纸，访客可切换 | **精简保留**。只提供 2 至 3 套经过统一调色的水系场景，并使用洛琪希素材衍生图；不复制原壁纸 | `替换` |
| A12 | 字体切换 | 在显示设置中切换字体，新增霞鹜文楷和 JetBrains Mono | **保留能力**。正文和界面只提供经过验证的两套组合，字体自托管并控制体积 | `保留` |
| A13 | 档案面板与布局切换 | Aemeath 增加归档筛选面板和单独布局切换组件，部分旧组件已标记废弃 | **沿用新版 Firefly** 的归档和集成式显示设置，只移植确有价值的筛选交互 | `替换` |
| A14 | 首页卡片主题装饰 | 根据标题、分类和标签选择不同 SVG 水印，悬停时增加彩色阴影 | **保留思路并重绘**。统一为 4 至 5 个水纹、魔法阵、星轨符号，避免原版互不相关的图案 | `替换` |
| A15 | 自定义鼠标 | 桌面端使用 `.cur` 文件替换默认和链接指针 | **暂缓**。优先保证可用性；若启用，只在精确指针设备使用轻量主题光标 | `暂缓` |
| A16 | 深度定制“关于”页 | 个人资料、项目卡、站点变更记录等内容大量硬编码 | **保留并重做**。作为角色图像最集中的主视觉页面，所有个人内容接入后台 | `保留` |
| A17 | 大型主题页脚 | 动物装饰、社交入口、建站时长、备案、访问统计、星球与波浪 | **保留结构密度，替换视觉**。改为水面、星屑和魔法回路；备案按实际情况，统计可选 | `替换` |
| A18 | Umami 统计展示 | 在页面或页脚展示访问数据 | **可选保留**。只在已有 Umami 配置时启用，不阻塞页面渲染 | `保留/暂缓` |
| A19 | AI 推荐语 | 本地展示文章摘要并做打字动画，但硬编码模型名称 | **替换**为“文章导读”，使用文章描述或后台摘要，不宣称由特定 AI 生成 | `替换` |
| A20 | Waline 深度定制 | 评论、邮件模板、管理预览和 AI 回复身份 | **删除**。当前博客明确不做评论互动，可显著减少依赖与维护面 | `删除` |
| A21 | 友链魔法入口与特殊身份 | 友链卡、截图、RSS、推荐标识和互动入口 | **暂缓**。第一版不让社交功能抢占核心阅读路径 | `暂缓` |
| A22 | Post Studio 本地发文工具 | 命令行交互发布、隔离坏文章 | **不替换现有后台**。远程中文后台仍是主发布方式，可把校验逻辑并入构建流程 | `替换` |
| A23 | 旧版代码块与 Markdown 插件改造 | Aemeath 自带代码交互、Mermaid 和 wiki-link 变体 | **使用新版 Firefly 原生实现**。新版已有 KaTeX、Mermaid、PlantUML、代码组、图片网格、GitHub 卡片等 | `Firefly 原生` |
| A24 | 自定义鼠标、樱花和 Three.js 依赖 | 视觉效果来源分散，Three.js 会增加首屏体积 | **不直接移植**。只为明确选中的水系动效加载所需代码，优先 CSS、SVG 和 Canvas 2D | `替换` |
| A25 | 作者专属静态资源 | 47 套壁纸源图/缩略图、角色、音乐、友链截图、页脚动物和文章配图 | **全部不使用**。仅使用用户提供并确认可展示的洛琪希素材和原博客媒体 | `删除` |

## 3. Firefly 6.16.6 应保留的新版能力

以下能力不从 Aemeath 回移旧实现：

- 文章、归档、分类、标签、系列、搜索和 RSS。
- `/dynamic` 个人动态，作为新站“说说”。
- 图库相册、瀑布流、低质量占位图和灯箱。
- 音乐播放器及新版 `MusicPlayerView`，在此基础上实现完整音乐页和歌词。
- Bangumi、Bilibili、MyAnimeList、VNDB 等现有内容接口；首版番剧数据仍建议由后台本地维护。
- KaTeX、Mermaid、PlantUML、wiki-link、图片网格、代码组、代码复制和 GitHub 卡片。
- Swup 页面转场及其组件清理机制。
- 当前响应式布局、显示设置、图片优化和无障碍基础。

## 4. 旧博客内容迁移证据

### 4.1 已确认可迁移内容

| 内容 | 当前数量 | 迁移结论 |
| --- | ---: | --- |
| Markdown 文章 | 10 篇 | 正文均可迁移 |
| 正文图片引用 | 45 个唯一引用 | 45 个文件全部存在，无缺失 |
| `public/uploads` 媒体 | 60 个文件 / 9.17 MB | 整体迁移，保留未被当前文章引用但可能仍在后台使用的文件 |
| LaTeX 块公式 | 12 个 `$$` 边界 | Firefly KaTeX 原生兼容 |
| Mermaid / PlantUML | 当前文章未使用 | 新站继续支持 |
| 脚注 / 文献引用 | 当前文章未实际使用 | Firefly 脚注可用；现有自定义参考文献字段先保留数据，后续决定是否接入新版渲染 |

### 4.2 Frontmatter 字段转换

| 旧字段 | Firefly 字段 | 转换规则 |
| --- | --- | --- |
| `title` | `title` | 原样保留 |
| `publishDate` | `published` | 原样转换为日期 |
| `updatedDate` | `updated` | 有值时保留 |
| `description` | `description` | 原样保留 |
| `excerpt` | 无直接对应 | 不重复塞入正文；后台保留为迁移记录，页面导读优先使用 `description` |
| `section: study` | `category: 学习笔记` | 映射为中文分类 |
| `section: research` | `category: 科研记录` | 映射为中文分类 |
| `section: life` | `category: 生活手账` | 映射为中文分类 |
| `featured` | `pinned` | `true` 转为置顶 |
| `draft` | `draft` | 原样保留，隐藏文章不会公开 |
| `sortOrder` | 无直接对应 | 不污染 Firefly schema；同日期人工排序时通过发布时间或新增的后台排序扩展解决 |
| `tags` | `tags` | 保留标签名称并校验标签管理数据 |
| `references` | 无直接对应 | 数据先保留在迁移备份；当前文章未使用 `[@key]`，不会造成正文损失 |

### 4.3 正文语法转换

旧文章共有 44 张图片使用 `![说明 | lg | center](...)` 一类私有指令。Firefly 会把整段当作替代文字，而不会理解 `lg/center`。迁移时执行以下规则：

1. 转为标准 Markdown 图片和 Firefly 图注结构，真实说明保留。
2. `center` 使用新主题统一的文章图片版式，不再逐图写方位指令。
3. `md/lg` 只在确有阅读意义时映射到新主题的窄图/宽图样式；论文图默认使用正文宽版。
4. 连续对比图可人工转换为 Firefly `[grid]...[/grid]`，不机械地把所有图片塞进网格。
5. 图片地址继续使用 `/uploads/...`，避免无必要的批量改名和链接失效。
6. 公式、粗体、斜体、列表、标题和普通链接保持原始 Markdown。

### 4.4 已完成的迁移验证

迁移工具位于 `scripts/prepare-firefly-migration.mjs`，默认只检查源内容；指定项目外的 `--output` 目录时才会写入独立迁移副本，不会覆盖“灯火版”。

验证过程使用 Firefly `6.16.6` 的真实依赖和生产构建链路，而不是仅检查文本：

| 验证项 | 结果 |
| --- | --- |
| `npm run verify:firefly-migration` | 10 篇文章、45 个媒体引用、60 个媒体文件全部通过；44 处旧图片指令完成转换；0 警告 |
| Firefly `astro check` | 249 个文件，0 errors、0 warnings、0 hints |
| Firefly 完整 `pnpm run build` | 成功生成 32 个页面，生产构建退出码 0 |
| 迁移文章路由 | 10/10 个文章页面均已生成 |
| 图片展示结构 | 45 个图片引用全部生成 `<figcaption>`，旧图片私有指令残留为 0 |
| 数学公式 | VTG-LLM 文章生成 KaTeX display/mathml/html 标记 |
| 搜索与订阅 | Pagefind 索引和 `rss.xml` 均已生成 |

构建期间 GitHub 示例卡片接口出现匿名请求限流，但 Firefly 使用缓存数据继续完成构建；该提示与迁移文章无关。正式新站会移除示例文章卡片数据，避免这项无关请求。

## 5. 洛琪希素材处理计划

当前素材目录已确认包含 3 个 MP4、1 个 GIF、多张 JPG，以及 Wallpaper Engine 的 `scene.pkg` 和着色器文件。

| 素材类型 | 使用方式 | 处理规则 |
| --- | --- | --- |
| `3466736816/洛琪希.mp4`（143.09 MB） | 首选开场动态候选 | 不进入 Git 仓库；转码为多档 WebM/MP4 后放 R2，并提供静态封面和移动端降级 |
| `3440522372/rochelle.mp4`（102.04 MB） | 备选开场或 About 环境动态 | 先截帧和试听/观看后决定，不与首屏同时加载 |
| `3362625868/ROXY@!!!_batch.mp4`（97.39 MB） | 备选片段 | 只在内容和画面风格一致时使用 |
| `3466736816/preview.gif` | 动态预览 | 不直接当首屏大背景；转换为 WebP/AVIF 或视频，避免 GIF 体积效率差 |
| 竖版洛琪希 JPG | 开场、首页局部和 About 主视觉 | 全角色只在少数关键页面出现；其余页面使用裁切、倒影、纹理和色彩呼应 |
| 方形预览 JPG | 首页章节封面、研究/阅读场景 | 统一做蓝青调色和响应式裁切 |
| `scene.pkg` / `.dxs` | Wallpaper Engine 工程文件 | 浏览器不能直接使用；只提取其可导出的画面，不把运行文件部署到网站 |

## 6. 推荐的第一版范围

如果按“主题统一、视觉足够丰富、后台可维护、上线风险可控”排序，建议第一版采用：

1. 首页：洛琪希动态开场、主题首页、精选文章和最新内容。
2. 归档：文章、分类、标签、搜索和系列。
3. 说说：使用 Firefly `/dynamic`，接入后台发布。
4. 图库：相册与灯箱，接入后台管理。
5. 音乐：新版播放器基础上做独立音乐页、播放列表、歌词和迷你播放器。
6. 番剧：本地后台集合、筛选、评分、进度和详情弹窗，不依赖 Bilibili/TMDB 构建接口。
7. 关于：洛琪希主视觉、个人资料、研究兴趣、爱好、当前状态和联系入口，全部后台可编辑。
8. 全站主题：水纹、魔法阵、星屑、景深与页面转场；支持动效降级和移动端适配。
9. 后台：沿用 GitHub + Decap CMS + Cloudflare Pages，重建与 Firefly 内容集合对应的中文字段。
10. 暂不公开：每日打卡、朋友圈、评论、工具、更新日志、服务器状态和复杂友链互动。

## 7. 需要用户确认的决策

请重点确认下列项目，其他项目可按本文件的默认建议执行：

1. A07：是否保留“不读取位置”的本地时间欢迎语。
2. A08/A09：侧栏选择“时间/天气”“每日一言”中的一个，还是两个都不要。
3. A11：是否允许访客在 2 至 3 套主题背景间切换。
4. A15：是否需要主题自定义鼠标。
5. A18：是否在第一版接入 Umami 访问统计。
6. A21：友链页第一版暂缓，还是保留一个简洁入口。
7. 番剧数据：确认采用“后台手工维护”为主，而不是绑定 Bilibili/TMDB。
8. 推荐第一版范围中的 10 项是否整体通过。
